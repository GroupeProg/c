#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "tp3.h"

int main()
{
    /* T_jour jour1 = 1;
    T_mois mois1 = 11;
    T_date date;
    date.jour = jour1;
    date.mois = mois1;
    date.njour = 15;
    date.annee = 1985;
    convert_jour(1);
    convert_mois(1);
    afficheDate(date); */
    // saisieDate();
    allocTabFloatPrompt();
    return 0;
}
